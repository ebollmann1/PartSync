<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <record id="res_config_settings_view_form" model="ir.ui.view">
            <field name="name">Settings</field>
            <field name="model">res.config.settings</field>
            <field name="priority" eval="40" />
            <field name="inherit_id" ref="sale.res_config_settings_view_form" />
            <field name="arch" type="xml">
                <xpath expr="//block[@name='catalog_setting_container']" position="after">
                    <block title="Report Configuration" name="catalog_setting_container">
                        <setting string="Configure Invoice Report Download After Sale Order Confirmation" help="Set up the configuration that automatically triggers the invoice report download after confirming a sale order.">
                            <field name="report_configuration_id"/>
                        </setting>
                    </block>
                </xpath>
            </field>
        </record>
    </data>
</odoo>